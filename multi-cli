#!/usr/bin/env bash
# Temporary compatibility shim. The Nini Agents engine lives in nini-agents.
set -e

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec "$SCRIPT_DIR/nini-agents" "$@"
